import { TransferColorDirective } from './transfer-color.directive';

describe('TransferColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TransferColorDirective();
    expect(directive).toBeTruthy();
  });
});
